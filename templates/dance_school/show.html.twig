{% extends 'base.html.twig' %}

{% block title %}DanceSchool{% endblock %}

{% block body %}
    <h1>DanceSchool</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dance_school.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ dance_school.name }}</td>
            </tr>
            <tr>
                <th>Localisation</th>
                <td>{{ dance_school.localisation }}</td>
            </tr>
            <tr>
                <th>Openday</th>
                <td>{{ dance_school.openday }}</td>
            </tr>
            <tr>
                <th>Closeday</th>
                <td>{{ dance_school.closeday }}</td>
            </tr>
            <tr>
                <th>Opentime</th>
                <td>{{ dance_school.opentime }}</td>
            </tr>
            <tr>
                <th>Closetime</th>
                <td>{{ dance_school.closetime }}</td>
            </tr>
            <tr>
                <th>Dance Category</th> <!-- Affichage de la catégorie de danse -->
                <td>{{ dance_school.danceCategory ? dance_school.danceCategory.name : 'N/A' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_dance_school_index') }}">back to list</a>

    <a href="{{ path('app_dance_school_edit', {'id': dance_school.id}) }}">edit</a>

    {{ include('dance_school/_delete_form.html.twig') }}
{% endblock %}
