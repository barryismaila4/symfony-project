<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Application Symfony{% endblock %}</title>

    {# Icone de l'application #}
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">

    {# Styles globaux #}
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f7f8fa;
            color: #333;
        }

        .dashboard-container {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .header {
            background-color: #2c3e50;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
        }
        
        .logo h1 {
            font-size: 1.8rem;
        }

        .navbar ul {
            display: flex;
            gap: 20px;
            list-style-type: none;
        }

        .navbar .nav-item {
            text-decoration: none;
            color: white;
            font-weight: 600;
            font-size: 1.1rem;
            padding: 10px;
            transition: background-color 0.3s ease;
        }

        .navbar .nav-item:hover, .navbar .nav-item.active {
            background-color: #3498db;
            border-radius: 5px;
        }

        .main-content {
            display: flex;
            flex: 1;
            margin-top: 20px;
            gap: 20px;
        }

        .sidebar {
            background-color: #34495e;
            color: white;
            padding: 20px;
            width: 250px;
            border-radius: 10px;
        }

        .sidebar h2 {
            font-size: 1.3rem;
            margin-bottom: 20px;
        }

        .sidebar ul {
            list-style-type: none;
        }

        .sidebar ul li {
            margin-bottom: 15px;
        }

        .sidebar ul li a {
            text-decoration: none;
            color: #bdc3c7;
            font-weight: 500;
            font-size: 1.1rem;
            transition: color 0.3s ease;
        }

        .sidebar ul li a:hover {
            color: #ecf0f1;
        }

        .content-area {
            flex: 1;
            background-color: white;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        .footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 20px 0;
            font-size: 1rem;
        }

        footer p {
            margin: 0;
            font-size: 14px;
        }

        .dance-image img {
            width: 500%;   /* Utilisez 100% pour qu'elle prenne la largeur du conteneur */
            max-width: 300px; /* Limiter la taille maximale de l'image */
            height: auto; /* Maintenir le ratio d'aspect */
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }

        @media (max-width: 768px) {
            .main-content {
                flex-direction: column;
                gap: 10px;
            }
            .sidebar {
                width: 100%;
            }
            .navbar ul {
                flex-direction: column;
                align-items: center;
            }
            .navbar .nav-item {
                margin-bottom: 10px;
            }
        }
    </style>

    {% block stylesheets %}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.1.0/css/bootstrap.min.css">
    {% endblock %}
    
    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</head>

<body>
    <div class="dashboard-container">
        <header class="header">
            <div class="logo">
                <h1>Backoffice - Gestion CRUD</h1>
            </div>

            <nav class="navbar">
                <ul>
                    <li><a href="{{ path('app_dance_category_index') }}" class="nav-item">Catégories</a></li>
                    <li><a href="{{ path('app_dance_school_index') }}" class="nav-item">Écoles de danse</a></li>
                    <li><a href="{{ path('app_course_index') }}" class="nav-item">Cours</a></li>
                </ul>
            </nav>
        </header>
        
        <div class="main-content">
            <aside class="sidebar">
                <h2>Actions rapides</h2>
                <ul>
                    <li><a href="{{ path('app_dance_category_new') }}">Ajouter Catégorie</a></li>
                    <li><a href="{{ path('app_dance_school_new') }}">Ajouter École</a></li>
                    <li><a href="{{ path('app_course_new') }}">Ajouter Cours</a></li>
                </ul>
            </aside>

            <section class="content-area">
                <div class="intro-container">
                    <h2 class="dance-title">Bienvenue dans l'Univers de la Danse</h2>
                    <p class="intro-text">Découvrez nos catégories, écoles et cours de danse qui vous inspireront à bouger !</p>
                    <div class="dance-image">
                        <img src="https://th.bing.com/th/id/R.e9c50e9f8517bb676f985a183d81637c?rik=nY2bA5KgatJ%2bFQ&riu=http%3a%2f%2fwww.phoenixdancetheatre.co.uk%2fwp-content%2fuploads%2f2014%2f01%2fDSC_1910.jpg&ehk=DF%2ff5IU7CijO2zr5z4OcosgibnLIXkXDbVTJPUuw4q8%3d&risl=1&pid=ImgRaw&r=0" alt="Image de danse" />
                    </div>
                </div>

                {% block body %}
                {% endblock %}
            </section>
        </div>

        <footer class="footer">
            <p>&copy; 2025 Site de Danse - Tous droits réservés</p>
        </footer>
    </div>

    {% block body_javascripts %}
    {% endblock %}
</body>
</html>

