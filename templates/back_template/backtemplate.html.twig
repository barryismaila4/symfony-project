{# templates/back_template/backtemplate.html.twig #}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Backoffice - Gestion CRUD</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
</head>
<body>
    <div class="dashboard-container">
        <header class="header">
            <div class="logo">
                <h1>Backoffice - Gestion CRUD</h1>
            </div>
            <nav class="navbar">
                <ul>
                    <li><a href="{{ path('app_back_template_categories') }}" class="nav-item">Catégories</a></li>
                    <li><a href="{{ path('app_back_template_schools') }}" class="nav-item">Écoles de danse</a></li>
                    <li><a href="{{ path('app_back_template_courses') }}" class="nav-item">Cours</a></li>
                </ul>
            </nav>
        </header>

        <div class="main-content">
            {% block content %}
            {# Le contenu spécifique à chaque section sera injecté ici #}
            {% endblock %}
        </div>

        <footer>
            <p>© 2025 Site de Danse - Tous droits réservés</p>
        </footer>
    </div>
</body>
</html>